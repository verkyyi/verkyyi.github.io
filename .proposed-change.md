---
title: remove-openai-harness-blog-from-evolve
type: workflow-fix
priority: P1
---

## Problem
The evolve.yml workflow prompt hardcodes `openai.com/index/harness-engineering/` as a research source (line 92: curl command, line 102: seed data). This URL has been Cloudflare-blocked for 103+ days. Every hourly evolve run checks it and logs a no-action entry, generating ~170 wasted research log entries per week (~2,800+ total).

Three prior removal attempts failed:
- PR #65 (research-source-cleanup): PHANTOM — only deleted .proposed-change.md
- PR #67 (issue-for-openai-blog-removal): merged but the issue was never created
- PR #70 (evolve-quiet-mode): PHANTOM — only deleted .proposed-change.md

The source was already removed from state/evolve_config.md Research Sources section, but remains hardcoded in the workflow YAML.

## Proposed Change
Edit `.github/workflows/evolve.yml` to:
1. Remove the curl command fetching `openai.com/index/harness-engineering/` (around line 92)
2. Remove the seed data reference to `openai-harness-blog` (around line 102)
3. Remove any other references to `openai-harness-blog` in the workflow file

## Expected Impact
- Eliminates ~170 wasted research log entries per week
- Reduces research_log growth by ~25%
- Removes a 103-day stale reference that has never returned useful data
- Saves ~4 API calls per hourly evolve run

## Risk
Low. The source has been non-functional for 103+ days. Removal was already approved in evolve_config.md. This is a workflow YAML change — PR should be labeled `needs-review` per autonomy rules.
