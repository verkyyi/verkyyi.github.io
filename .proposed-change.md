# Proposed Change: research-source-cleanup

## Summary
Remove `openai-harness-blog` from the discover/evolve research source check list. The blog has been Cloudflare-blocked for 97+ consecutive days, generating ~4 useless "no action" entries per hourly evolve run. This is pure waste — no content has ever been retrieved from this source.

## Motivation
- openai-harness-blog has been Cloudflare-blocked since at least 2026-01-05 (97+ days)
- Every hourly evolve run checks it and logs "Cloudflare-blocked — no content accessible"
- This generates ~96 wasted research_log entries per day (~4 per run × 24 runs)
- research_log is growing at ~14KB/day; removing this source would cut that by ~25%
- The source was already removed from evolve_config.md (PR #14, 2026-03-26) but is still checked by the evolve workflow's hardcoded source list

## Changes Required
1. In the evolve workflow (`.github/workflows/evolve.yml`), remove `openai-harness-blog` from any hardcoded research source lists or curl targets
2. In the discover workflow if applicable, remove the same source
3. No functional impact — the source has never returned useful content

## Risk Assessment
- **Risk**: None. Source has been inaccessible for 97+ days.
- **Reversibility**: Trivially re-addable if the blog becomes accessible.
- **Label**: `auto-merge` (low-risk configuration cleanup)

## Expected Impact
- ~25% reduction in research_log growth rate
- ~96 fewer wasted API calls per day
- Extends time between required log truncations

## Source
Weekly analysis W31 (2026-04-12). Priority P1 in project_state.md.
