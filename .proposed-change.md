# Proposed Change: Update evolve_config.md for tokenman rename

## Summary
Update `state/evolve_config.md` to reflect the upstream repo rename from `agentfolio` to `tokenman` and add the current version (v0.3.0).

## Motivation
- The upstream scaffold repo `verkyyi/agentfolio` was renamed to `verkyyi/tokenman` and has released v0.3.0 "Lean Operations"
- The current config still references `verkyyi/agentfolio` in Research Sources
- No Version field exists in the config, making it impossible to track upstream alignment
- This has been a P1 recommendation for 3+ consecutive weekly analyses (weeks 3, 4, 5)
- tokenman v0.3.0 includes cron optimization patterns that align with our PR #15 fix — staying current with upstream helps identify relevant improvements

## Changes
In `state/evolve_config.md`:
1. Update Research Sources: `verkyyi/agentfolio` → `verkyyi/tokenman`
2. Add `- **Version:** v0.3.0` to the Repo Profile section
3. Update Last recheck date to 2026-03-26

## Risk
Low — config metadata only, no behavioral changes, no workflow YAML modifications.

## Labels
auto-merge
