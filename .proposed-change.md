# Proposed Change: evolve-config-stale-source

## Summary
Update the research source in `state/evolve_config.md` from `verkyyi/agentfolio` to `verkyyi/tokenman`. The repo was renamed 2026-04-07; every evolve research check now follows a 301 redirect, wasting API calls and logging stale-config warnings ~24x/day.

## Change Type
State file update (AUTO tier per CLAUDE.md autonomy rules)

## Files Modified
- `state/evolve_config.md`: Replace `verkyyi/agentfolio` with `verkyyi/tokenman` in Research Sources section

## Risk
None — cosmetic config correction. The 301 redirect already resolves to tokenman, so current behavior is functionally correct but noisy. Eliminates ~24 "config stale" warnings per day in research_log.

## Expected Impact
- Eliminates 301 redirect overhead on every evolve research check
- Removes "config source stale" warnings from agent_log and research_log
- Reduces log growth by ~1-2KB/day (stale-config warning lines)

## Origin
Weekly analysis W23 (2026-04-09T12:26Z). Finding first reported 2026-04-07T18:21Z in research_log. Confirmed by 30+ subsequent evolve runs logging the stale redirect. Listed as P1 priority in project_state.md since W21.
