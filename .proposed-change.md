---
name: issue-pathway-for-log-truncation
description: Create a GitHub issue requesting recurring log truncation so the coder agent can execute it directly
type: workflow-behavior
tier: AUTO
reason: |
  Log truncation has failed 5 times via the .proposed-change.md→PR pathway:
  - PRs #39, #42, #48, #50, #51: merge-blocked by conflicts (9+ weeks)
  - PR #59: phantom-merged (coder created branch with .proposed-change.md file but never truncated logs)
  
  The W28 analysis resolved the immediate crisis via direct state commit (agent_log 616→140KB,
  research_log 276→28KB), but logs will regrow at ~100 entries/day (~200 entries/day combined).
  Without recurring truncation, both logs will exceed the 256KB Read limit again within ~2-3 weeks.
  
  The only proven implementation pathway is: GitHub Issue → Triage → Coder Agent → Direct Edit.
  This was validated by Issue #57 / PR #58 (evolve_config fix, <3h end-to-end).
  
  Proposed: The analyze workflow should create a GitHub issue labeled "auto-fix" when logs exceed
  a threshold (e.g., 400 entries or 200KB), requesting truncation to the most recent 200 entries.
  The coder agent will pick up the issue and perform the truncation as a direct state commit.
---

## Problem

Log files regrow at ~100-200 entries per day. Without recurring truncation, both agent_log.md
and research_log.md will exceed the 256KB Read tool limit again within 2-3 weeks, making them
unreadable by automation.

The .proposed-change.md pathway is broken (phantom PR pattern — 5/6 failures confirmed).
The PR pathway is blocked (5 archival PRs merge-blocked 9+ weeks by cron-induced conflicts).

## Solution

Use the issue→coder pathway (only proven working implementation path):

1. In analyze.yml: when agent_log.md or research_log.md exceeds 400 entries, create a GitHub
   issue labeled `auto-fix` requesting truncation to the most recent 200 entries
2. Triage workflow labels and assigns the issue
3. Coder agent executes truncation as a direct state commit (AUTO-tier per CLAUDE.md)
4. Issue is closed automatically when truncation is verified

This reuses the validated watcher→issue→triage→coder pipeline that resolved Issue #57 in <3h.

## Risk

LOW — state file truncation is explicitly AUTO-tier in CLAUDE.md. The issue→coder pathway
has been validated end-to-end. Old log entries are archival (no operational dependency).

## Validation

- agent_log.md and research_log.md remain under 256KB after truncation
- Entries from the current week are preserved
- No operational data is lost (old entries are state churn records)
