# Proposed Change: evolve-config-direct-fix

## Summary
Directly update the research source in `state/evolve_config.md` from `verkyyi/agentfolio` to `verkyyi/tokenman`. PR #54 (evolve-config-stale-source) was merged on 2026-04-09 but only created a `.proposed-change.md` file — it did NOT update the actual config. The config still references the old repo name, causing a 301 redirect on every research check (79+ days of unnecessary redirects).

## Change
**File:** `state/evolve_config.md`
**Section:** Research Sources
**From:** `- verkyyi/agentfolio`
**To:** `- verkyyi/tokenman`

## Justification
- `verkyyi/agentfolio` returns HTTP 301 redirect to `verkyyi/tokenman` since at least 2026-04-07
- Every evolve run (hourly) follows this redirect, wasting API calls
- PR #54 was supposed to fix this but only created a proposed-change file, not the actual config edit
- This is a state file update (AUTO tier per CLAUDE.md autonomy rules)
- Zero risk — the repo is confirmed renamed, the redirect is permanent

## Risk
None. The old name already 301-redirects to the new name. This change eliminates the redirect hop.

## Tier
AUTO (state file update) or PR with auto-merge label (config change)
