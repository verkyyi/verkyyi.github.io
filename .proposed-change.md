# Proposed Change: Direct AUTO-tier commits for FEATURE_STATUS.md and evolve_config.md

## Problem
FEATURE_STATUS.md has been recommended for 8 consecutive weeks with 7 PR attempts (PRs #26/#29/#31/#32/#33 this week alone), ALL failing or misleading. The PR route is structurally broken: hourly cron produces ~99 state commits/day, creating merge conflicts on PR branches before reviewer can act. Similarly, evolve_config.md updates (tokenman rename, v0.3.0 reference) failed via PR twice (#17/#30).

Both files are AUTO-tier per CLAUDE.md autonomy rules:
- "FEATURE_STATUS updates" → AUTO (commit directly, no PR needed)
- "State file updates" → AUTO

## Solution
Evolve workflow should commit these files directly to main instead of opening PRs:

1. **Create `state/FEATURE_STATUS.md`** with current feature tracking:
   - Issue #2: Landing page (IN PROGRESS — PR #4 blocked on merge conflicts)
   - Issue #24: Awesome-list submission (BLOCKED — needs repo topics + landing page)
   - Cron frequency reduction (BLOCKED — requires human manual edit)
   - README cleanup (DONE — PR #21 merged 03-27)

2. **Update `state/evolve_config.md`** Research Sources section:
   - Rename `verkyyi/agentfolio` → `verkyyi/tokenman`
   - Add note: v0.3.0 "Lean Operations" released 2026-03-26

## Impact
- Eliminates the #1 recurring analysis recommendation (8 weeks)
- Removes a source of wasted PR churn (7 failed PRs = 7 reviewer runs + 7 watcher triggers)
- Enables feature progress tracking that every other workflow can reference
- Updates stale config that causes research log noise

## Risk
LOW — both changes are explicitly AUTO-tier per CLAUDE.md. No behavioral changes, no workflow YAML modifications, no autonomy rule changes.

## Labels
project:verkyyi.github.io
