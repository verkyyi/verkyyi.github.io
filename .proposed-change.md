---
name: node-24-migration-issue
type: issue
priority: P2
---

## Node.js 20→24 Migration

Create a GitHub issue for the coder agent to update Node.js version from 20 to 24 in all GitHub Actions workflow YAML files.

### Context
- GitHub Actions forcing Node.js 24 by June 2026
- ~4 weeks remaining — escalated from P3 to P2 at W34 analysis
- Affects all workflows using `actions/setup-node` or `node-version`
- Issue→coder pathway is the only proven mechanism for code changes (#57→#58 validated)

### Changes Needed
- Update `node-version` from '20' to '24' in all workflow YAML files
- Verify workflows pass with Node 24

### Risk
Low — Node 24 is the upstream-required version. GitHub Actions will force the migration regardless. Early migration avoids a forced, potentially breaking cutover.
